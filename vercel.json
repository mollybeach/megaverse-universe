{
    "buildCommand": "pnpm run build",
    "framework": "nextjs",
    "installCommand": "pnpm install",
    "builds": [{
        "src": "next.config.js",
        "use": "@vercel/next"
    }],
    "build": {
        "env": {
            "NODE_ENV": "production"
        }
    }
}